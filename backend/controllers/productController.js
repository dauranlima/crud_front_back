const {Product: ProductModel} = require("../models/product")

const productController = () => {

create: async (req, res) => {
  try {
    
    const product = {
    }
  } catch (err) {
    console.log(err)
  }
}






}